import { IdentityReveledDirective } from './identity-reveled.directive';

describe('IdentityReveledDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityReveledDirective();
    expect(directive).toBeTruthy();
  });
});
